server:
  port: ${SERVER_PORT:0}
  shutdown: graceful
spring:
  application:
    name: ${APP_NAME:grocery-product}
  profiles:
    active: ${ENV:dev}
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/grocery_product
    username: root
    password: nmtkma0550
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false
    properties:
      format_sql: true
    hibernate:
      ddl-auto: update
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/grocery-realm
          jwk-set-uri: http://localhost:8180/realms/grocery-realm/protocol/openid-connect/certs

cloudinary:
  product-url:
    regex: https://res.cloudinary.com/dtcdff7yy/image/upload
    resized-regex: https://res.cloudinary.com/dtcdff7yy/image/upload/c_fill,w_350,h_350

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
